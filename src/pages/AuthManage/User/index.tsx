import { useState } from 'react';
import { Card, Form } from 'antd';

const UserList: React.FC = () => {
  return <div>
    <Card bordered={false}>
      
    </Card>
  </div>;
};

export default UserList;
